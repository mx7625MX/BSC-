# Web控制面板使用指南

## 🌐 Web界面简介

BSC MEME币阻击机器人现在包含一个简洁的Web控制面板，让您可以通过浏览器实时监控机器人状态。

## ✨ 功能特点

- **实时状态显示** - 查看机器人运行状态、钱包余额、已监控的交易对数量
- **功能状态监控** - 一目了然地查看所有功能的启用状态（自动交易、止盈止损、快速卖出等）
- **持仓管理** - 实时显示当前持有的所有代币
- **实时日志** - 通过WebSocket推送实时日志，无需刷新页面
- **中文界面** - 完全中文化的界面，易于理解
- **响应式设计** - 支持电脑和手机浏览器

## 🚀 启动方式

### 1. 安装依赖

```bash
npm install
```

这会自动安装Web界面所需的Express和WebSocket依赖。

### 2. 编译代码

```bash
npx tsc
```

### 3. 启动机器人

```bash
npm start
```

或者直接运行：

```bash
node dist/index.js
```

### 4. 访问Web界面

机器人启动后，在浏览器中访问：

```
http://localhost:3000
```

## 📊 界面说明

### 基本信息卡片
- **钱包地址** - 显示当前使用的钱包地址（前10位）
- **账户余额** - 实时显示BNB余额
- **已监控交易对** - 显示自启动以来检测到的交易对数量

### 功能状态卡片
显示所有可用功能的启用状态：
- 🟢 绿色指示灯 = 功能已启用
- 🔴 红色指示灯 = 功能未启用

### 交易配置卡片
显示当前的交易参数配置：
- 最大购买金额（MAX_BUY_AMOUNT）
- 最小流动性要求（MIN_LIQUIDITY_BNB）

### 当前持仓
以表格形式显示所有持有的代币：
- 代币地址
- 持有数量
- 买入价格
- 购买时间

### 实时日志
显示最近的50条日志记录，包括：
- INFO - 普通信息
- WARN - 警告信息
- ERROR - 错误信息

日志通过WebSocket实时推送，无需手动刷新。

## ⚙️ 配置端口

默认端口是3000。如果需要修改，可以编辑 `src/index.ts` 文件：

```typescript
const webServer = new WebServer(3000); // 修改这里的端口号
```

## 🔒 安全提示

1. **局域网访问** - Web界面默认只能从localhost访问
2. **生产环境** - 如果需要远程访问，建议配置反向代理（如Nginx）并启用HTTPS
3. **防火墙** - 如果开放远程访问，请配置防火墙规则限制访问来源

## 📱 移动设备访问

如果需要从手机访问Web界面：

1. 确保手机和运行机器人的电脑在同一局域网
2. 找到电脑的IP地址（如 192.168.1.100）
3. 在手机浏览器中访问：`http://192.168.1.100:3000`

**注意**: 需要修改 `src/webServer.ts` 中的监听地址：

```typescript
this.server.listen(this.port, '0.0.0.0', () => {
  // ...
});
```

## 🎯 使用建议

1. **测试模式** - 首次使用时，建议设置 `AUTO_TRADE_ENABLED=false` 进行测试
2. **实时监控** - 将浏览器窗口保持打开，随时查看机器人状态
3. **日志分析** - 通过日志了解机器人的工作情况，判断是否需要调整配置

## 🐛 常见问题

### 无法访问Web界面

1. 检查机器人是否正常启动
2. 确认控制台是否显示 "Web控制面板已启动" 消息
3. 检查端口3000是否被其他程序占用
4. 尝试使用 `http://127.0.0.1:3000` 访问

### WebSocket连接失败

1. 检查浏览器控制台是否有错误信息
2. 确认防火墙没有阻止WebSocket连接
3. 尝试刷新页面重新连接

### 数据不更新

1. 检查WebSocket连接状态（浏览器F12开发者工具）
2. 查看控制台日志是否有错误
3. 尝试刷新页面

## 📝 技术说明

- **后端**: Express.js + WebSocket (ws)
- **前端**: 原生HTML/CSS/JavaScript
- **通信**: REST API + WebSocket实时推送
- **更新频率**: 状态每5秒广播一次，日志实时推送

## 🔄 未来功能

计划添加的功能：
- 手动执行交易控制
- 配置参数在线修改
- 交易历史记录
- 性能统计图表
- 多机器人管理

## 📞 技术支持

如果遇到问题，请在GitHub Issues中提问，或查看其他文档文件：
- `BEGINNER_GUIDE.md` - 新手入门指南
- `CONFIGURATION_GUIDE.md` - 配置详细说明
- `FAQ.md` - 常见问题解答
