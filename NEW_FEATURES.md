# 新功能说明：止盈止损 & 快速卖出 & 买入量触发卖出 & 钱包跟单

## 📊 功能1：自动止盈止损（可自由调节）

### 功能说明

机器人可以自动监控您持有的代币，当达到盈利目标或触发止损线时，自动卖出代币。**所有百分比都可以自由设置，没有固定限制。**

### 配置参数

在 `.env` 文件中添加以下配置：

```env
# 是否启用自动止盈 (true/false)
TAKE_PROFIT_ENABLED=true

# 止盈百分比 (可自由设置任意值，例如50表示盈利50%，200表示盈利200%)
TAKE_PROFIT_PERCENT=50

# 止损百分比 (可自由设置任意值，例如30表示亏损30%)
STOP_LOSS_PERCENT=30

# 检查盈亏间隔 (毫秒，建议60000即1分钟)
CHECK_PROFIT_INTERVAL=60000
```

### 参数说明

| 参数 | 说明 | 默认值 | 示例 |
|------|------|--------|------|
| `TAKE_PROFIT_ENABLED` | 是否启用止盈止损功能 | `false` | `true` |
| `TAKE_PROFIT_PERCENT` | 达到多少盈利百分比时自动卖出（**可自由设置**） | `50` | `50`, `100`, `200` 等任意值 |
| `STOP_LOSS_PERCENT` | 达到多少亏损百分比时自动卖出（**可自由设置**） | `30` | `20`, `50` 等任意值 |
| `CHECK_PROFIT_INTERVAL` | 多久检查一次盈亏（毫秒） | `60000` | `60000`（1分钟）|

### 使用示例

#### 示例1：保守止盈止损
```env
TAKE_PROFIT_ENABLED=true
TAKE_PROFIT_PERCENT=30        # 盈利30%就卖
STOP_LOSS_PERCENT=20          # 亏损20%就止损
CHECK_PROFIT_INTERVAL=30000   # 每30秒检查一次
```

#### 示例2：激进止盈止损（高收益目标）
```env
TAKE_PROFIT_ENABLED=true
TAKE_PROFIT_PERCENT=200       # 盈利200%（3倍）才卖
STOP_LOSS_PERCENT=50          # 亏损50%止损
CHECK_PROFIT_INTERVAL=120000  # 每2分钟检查一次
```

#### 示例3：仅止盈，不止损
```env
TAKE_PROFIT_ENABLED=true
TAKE_PROFIT_PERCENT=50        # 盈利50%卖出
STOP_LOSS_PERCENT=999         # 设置极大值，实际不会触发
CHECK_PROFIT_INTERVAL=60000
```

### 工作原理

1. **自动记录**：当机器人购买代币后，自动记录购买价格和数量
2. **定期检查**：按设定的间隔（如1分钟）检查每个持仓的盈亏
3. **计算盈亏**：获取当前代币价格，计算盈利/亏损百分比
4. **自动卖出**：
   - 如果盈利达到 `TAKE_PROFIT_PERCENT`，自动卖出
   - 如果亏损达到 `STOP_LOSS_PERCENT`，自动止损卖出
5. **记录日志**：所有操作都会详细记录在日志中

### 日志示例

```
[info]: 已记录持仓信息 { token: '0x...' }
[debug]: 持仓检查 { token: '0x...', profitPercent: '35.50%', ... }
[info]: 触发止盈！ { token: '0x...', profitPercent: '52.30%' }
[info]: 准备卖出代币 { token: '0x...', amount: '...' }
[info]: 卖出成功 { hash: '0x...', amountReceived: '0.15 BNB' }
```

### ⚠️ 重要提醒

1. **需要授权**：首次卖出某个代币时，需要先授权，会消耗额外的Gas
2. **Gas成本**：每次卖出都需要支付Gas费，频繁卖出会增加成本
3. **价格波动**：加密货币价格波动剧烈，设置合理的止盈止损点位
4. **检查间隔**：
   - 间隔太短：检查频繁，消耗更多资源
   - 间隔太长：可能错过最佳卖出时机
   - 建议：30秒 - 2分钟

---

## 👥 功能2：钱包跟单

### 功能说明

监控其他钱包的交易，当目标钱包购买代币时，机器人自动跟随购买相同或按比例的代币。

### 配置参数

在 `.env` 文件中添加以下配置：

```env
# 是否启用跟单功能 (true/false)
COPY_TRADE_ENABLED=true

# 监控的钱包地址 (用逗号分隔，跟随这些地址的交易)
MONITORED_WALLETS=0x1234...,0x5678...,0xabcd...

# 跟单金额 (BNB，每次跟单使用的固定金额)
COPY_TRADE_AMOUNT=0.05

# 跟单倍数 (根据目标钱包的购买金额按比例跟单)
COPY_TRADE_MULTIPLIER=1.0
```

### 参数说明

| 参数 | 说明 | 默认值 | 示例 |
|------|------|--------|------|
| `COPY_TRADE_ENABLED` | 是否启用跟单功能 | `false` | `true` |
| `MONITORED_WALLETS` | 要监控的钱包地址列表 | - | `0x123...,0x456...` |
| `COPY_TRADE_AMOUNT` | 固定跟单金额（BNB） | `0.05` | `0.1` |
| `COPY_TRADE_MULTIPLIER` | 跟单倍数 | `1.0` | `0.5`（一半）或 `2.0`（双倍）|

### 跟单模式

#### 模式1：固定金额跟单
```env
COPY_TRADE_AMOUNT=0.1      # 每次固定用0.1 BNB跟单
COPY_TRADE_MULTIPLIER=1.0  # 忽略此参数
```

**特点**：
- 无论目标钱包买多少，您都用固定金额跟单
- 风险可控，适合新手

**示例**：
- 目标钱包买 1 BNB → 您买 0.1 BNB
- 目标钱包买 0.5 BNB → 您买 0.1 BNB
- 目标钱包买 0.05 BNB → 您买 0.1 BNB

#### 模式2：按倍数跟单
```env
COPY_TRADE_AMOUNT=0        # 设为0表示使用倍数模式
COPY_TRADE_MULTIPLIER=0.5  # 用目标钱包金额的50%跟单
```

**特点**：
- 根据目标钱包的购买金额按比例跟单
- 更灵活，但风险也随之变化

**示例**（倍数0.5）：
- 目标钱包买 1 BNB → 您买 0.5 BNB
- 目标钱包买 0.5 BNB → 您买 0.25 BNB
- 目标钱包买 0.1 BNB → 您买 0.05 BNB

**示例**（倍数2.0）：
- 目标钱包买 0.1 BNB → 您买 0.2 BNB
- 目标钱包买 0.05 BNB → 您买 0.1 BNB

### 使用示例

#### 示例1：跟随知名地址（固定金额）
```env
COPY_TRADE_ENABLED=true
# 假设这是某个成功交易者的地址
MONITORED_WALLETS=0x1234567890123456789012345678901234567890
COPY_TRADE_AMOUNT=0.05     # 每次固定0.05 BNB
COPY_TRADE_MULTIPLIER=1.0
```

#### 示例2：跟随多个地址（按比例）
```env
COPY_TRADE_ENABLED=true
# 监控多个地址
MONITORED_WALLETS=0x123...,0x456...,0x789...
COPY_TRADE_AMOUNT=0        # 设为0启用倍数模式
COPY_TRADE_MULTIPLIER=0.3  # 用目标金额的30%跟单
```

#### 示例3：保守跟单
```env
COPY_TRADE_ENABLED=true
MONITORED_WALLETS=0x123...
COPY_TRADE_AMOUNT=0.02     # 小额固定跟单
COPY_TRADE_MULTIPLIER=1.0
MAX_BUY_AMOUNT=0.05        # 限制最大金额
```

### 工作原理

1. **监控交易**：机器人每隔一段时间（`MONITOR_INTERVAL`）检查最新区块
2. **识别目标**：查找目标钱包地址的交易
3. **过滤交易**：只处理发送到PancakeSwap Router的交易（购买交易）
4. **解析交易**：分析交易内容，获取购买的代币和金额
5. **计算跟单**：根据配置（固定金额或倍数）计算跟单金额
6. **执行跟单**：自动购买相同的代币

### 日志示例

```
[info]: 启动钱包跟单监控... { wallets: ['0x123...'] }
[info]: 检测到目标钱包购买交易 { from: '0x123...', hash: '0x...', value: '0.5 BNB' }
[info]: 准备跟单 { originalAmount: 0.5, copyAmount: 0.1, multiplier: 0.2 }
[info]: 跟单购买成功 { token: '0xabc...', amount: '0.1 BNB' }
```

### ⚠️ 重要提醒

1. **选择目标**：
   - 选择经过验证的成功交易者
   - 不要盲目跟随不知名地址
   - 研究目标钱包的历史交易记录

2. **延迟问题**：
   - 跟单有延迟，可能错过最佳价格
   - 目标钱包可能使用更快的节点
   - 考虑提高 `GAS_PRICE_MULTIPLIER` 加快交易

3. **风险控制**：
   - 设置合理的 `MAX_BUY_AMOUNT` 限制
   - 使用固定金额模式更安全
   - 不要投入大额资金

4. **合法性**：
   - 跟单是合法的链上行为
   - 但不保证盈利
   - 自行承担所有风险

5. **技术限制**：
   - 当前版本仅支持基础跟单
   - 需要进一步优化交易解析
   - 复杂交易可能无法正确识别

---

## 🔄 两个功能结合使用

### 完整配置示例

```env
# ========== 基础配置 ==========
PRIVATE_KEY=your_private_key_here
AUTO_TRADE_ENABLED=true
MAX_BUY_AMOUNT=0.2
MIN_LIQUIDITY_BNB=5

# ========== 止盈止损配置 ==========
TAKE_PROFIT_ENABLED=true
TAKE_PROFIT_PERCENT=50         # 盈利50%卖出
STOP_LOSS_PERCENT=30           # 亏损30%止损
CHECK_PROFIT_INTERVAL=60000    # 每分钟检查

# ========== 钱包跟单配置 ==========
COPY_TRADE_ENABLED=true
MONITORED_WALLETS=0x1234567890123456789012345678901234567890
COPY_TRADE_AMOUNT=0.05         # 固定0.05 BNB跟单
COPY_TRADE_MULTIPLIER=1.0
```

### 工作流程

1. 机器人启动后，同时运行三个模块：
   - 新币监控（原有功能）
   - 止盈止损监控（新功能）
   - 钱包跟单监控（新功能）

2. 当检测到交易机会时：
   - 新币上线 → 自动购买 → 记录持仓 → 定期检查盈亏
   - 目标钱包购买 → 跟单购买 → 记录持仓 → 定期检查盈亏

3. 止盈止损自动执行：
   - 所有持仓（新币或跟单）都会被监控
   - 达到止盈/止损条件自动卖出
   - 清除持仓记录

---

## 📚 常见问题

### Q: 止盈止损会自动卖出所有代币吗？
A: 是的，当触发条件时，会卖出该代币的全部持仓。

### Q: 可以手动卖出部分代币吗？
A: 目前不支持部分卖出，需要手动操作或停止机器人。

### Q: 跟单会跟目标钱包的卖出操作吗？
A: 当前版本仅跟随买入操作，不跟随卖出。

### Q: 如何找到值得跟单的钱包地址？
A: 
- 使用 BSCScan 分析历史成功交易
- 关注社区推荐的地址
- 观察代币的早期购买者
- ⚠️ 务必自己研究，不要盲目跟随

### Q: 两个功能可以单独使用吗？
A: 可以！您可以：
- 只启用止盈止损：`TAKE_PROFIT_ENABLED=true`，`COPY_TRADE_ENABLED=false`
- 只启用跟单：`TAKE_PROFIT_ENABLED=false`，`COPY_TRADE_ENABLED=true`
- 都启用：两个都设为 `true`
- 都不启用：保持原有功能

### Q: 检查盈亏的间隔应该设置多少？
A: 建议：
- 快速：30秒（`30000`）- 更及时但消耗更多资源
- 标准：1分钟（`60000`）- 推荐
- 慢速：2-5分钟（`120000-300000`）- 节省资源

### Q: 跟单的最大金额如何限制？
A: 跟单金额会自动受到 `MAX_BUY_AMOUNT` 限制，不会超过这个值。

---

## 🎯 最佳实践

### 止盈止损策略

1. **保守策略**
   ```env
   TAKE_PROFIT_PERCENT=30
   STOP_LOSS_PERCENT=20
   ```
   
2. **平衡策略**
   ```env
   TAKE_PROFIT_PERCENT=50
   STOP_LOSS_PERCENT=30
   ```

3. **激进策略**
   ```env
   TAKE_PROFIT_PERCENT=100
   STOP_LOSS_PERCENT=50
   ```

### 跟单策略

1. **新手策略**
   - 固定小额跟单：`COPY_TRADE_AMOUNT=0.02`
   - 监控1-2个经过验证的地址
   - 设置低的 `MAX_BUY_AMOUNT`

2. **进阶策略**
   - 按比例跟单：`COPY_TRADE_MULTIPLIER=0.5`
   - 监控3-5个地址
   - 结合止盈止损自动管理

3. **高级策略**
   - 多个地址不同倍数（需修改代码）
   - 结合其他分析指标
   - 自定义跟单逻辑

---

## ⚡ 功能3：快速卖出（1-5秒内自动卖出）

### 功能说明

购买代币后，在指定的时间范围内（1000-5000毫秒，即1-5秒）自动卖出。**适合快进快出的超短线交易策略。**

### 应用场景

1. **抢先交易**：在新币上线后快速买入卖出，赚取初期波动利润
2. **套利策略**：利用价格瞬间差异快速套利
3. **测试交易**：快速测试代币的流动性和滑点
4. **避免风险**：快速进出，减少持仓时间，降低风险

### 配置参数

在 `.env` 文件中添加以下配置：

```env
# 是否启用快速卖出 (true/false)
QUICK_SELL_ENABLED=true

# 最小延迟时间 (毫秒)
# 购买后最少等待多久才卖出，默认1000毫秒（1秒）
QUICK_SELL_DELAY_MIN=1000

# 最大延迟时间 (毫秒)
# 购买后最多等待多久就卖出，默认5000毫秒（5秒）
# 实际卖出时间会在最小和最大延迟之间随机选择
QUICK_SELL_DELAY_MAX=5000
```

### 参数说明

| 参数 | 说明 | 默认值 | 示例 |
|------|------|--------|------|
| `QUICK_SELL_ENABLED` | 是否启用快速卖出 | `false` | `true` |
| `QUICK_SELL_DELAY_MIN` | 最小延迟（毫秒）（**可自由设置**） | `1000` | `500`, `1000`, `2000` |
| `QUICK_SELL_DELAY_MAX` | 最大延迟（毫秒）（**可自由设置**） | `5000` | `3000`, `5000`, `10000` |

### 使用示例

#### 示例1：极速交易（1-2秒）
```env
QUICK_SELL_ENABLED=true
QUICK_SELL_DELAY_MIN=1000    # 最少1秒
QUICK_SELL_DELAY_MAX=2000    # 最多2秒
```

**特点**：
- 极快进出
- 适合抢先交易
- 风险低但利润也可能较小

#### 示例2：标准快速（2-5秒）
```env
QUICK_SELL_ENABLED=true
QUICK_SELL_DELAY_MIN=2000    # 最少2秒
QUICK_SELL_DELAY_MAX=5000    # 最多5秒
```

**特点**：
- 平衡速度和收益
- 给价格一些上涨空间
- 适合大多数快速交易场景

#### 示例3：稍缓交易（3-8秒）
```env
QUICK_SELL_ENABLED=true
QUICK_SELL_DELAY_MIN=3000    # 最少3秒
QUICK_SELL_DELAY_MAX=8000    # 最多8秒
```

**特点**：
- 给价格更多上涨时间
- 可能获得更高利润
- 但也增加了风险

#### 示例4：自定义范围
```env
QUICK_SELL_ENABLED=true
QUICK_SELL_DELAY_MIN=1500    # 1.5秒
QUICK_SELL_DELAY_MAX=4500    # 4.5秒
```

### 工作原理

1. **购买完成**：机器人成功购买代币后
2. **记录持仓**：记录代币地址、数量等信息
3. **计算延迟**：在最小和最大延迟之间随机选择一个时间
4. **设置定时器**：设置定时器在指定时间后卖出
5. **检查持仓**：到时间后，检查是否还持有该代币（可能已被其他功能卖出）
6. **执行卖出**：如果还持有，立即卖出全部持仓
7. **记录结果**：记录卖出结果和实际延迟时间

### 日志示例

```
[info]: 交易成功 { hash: '0xabc...', gasUsed: 250000 }
[info]: 已记录持仓信息 { token: '0xdef...' }
[info]: 已设置快速卖出 { 
  token: '0xdef...', 
  delay: '3247ms',
  minDelay: '1000ms',
  maxDelay: '5000ms' 
}
[info]: 执行快速卖出 { 
  token: '0xdef...', 
  amount: '1000000000000000000',
  actualDelay: '3247ms' 
}
[info]: 准备卖出代币 { token: '0xdef...', amount: '...' }
[info]: 卖出成功 { 
  hash: '0x456...', 
  amountReceived: '0.105 BNB' 
}
[info]: 快速卖出成功 { 
  token: '0xdef...', 
  hash: '0x456...',
  delay: '3247ms' 
}
```

### ⚠️ 重要提醒

1. **随机延迟**：
   - 实际卖出时间在最小和最大延迟之间随机
   - 避免所有交易在同一时间卖出
   - 更自然，不易被检测

2. **Gas费用**：
   - 快速交易需要高Gas价格
   - 建议提高 `GAS_PRICE_MULTIPLIER`（如2.0-3.0）
   - 确保交易能快速打包

3. **价格波动**：
   - 1-5秒内价格可能波动很大
   - 可能盈利也可能亏损
   - MEME币波动尤其剧烈

4. **滑点设置**：
   - 快速交易建议提高滑点容忍度
   - 推荐 `SLIPPAGE_TOLERANCE=15-20`
   - 避免因滑点过小导致交易失败

5. **与其他功能的关系**：
   - 快速卖出优先级最高
   - 设置定时器后，到时间就会卖出
   - 如果在此期间被其他功能卖出，定时器会自动取消

6. **交易成本**：
   - 快速进出会产生双倍Gas费用（买+卖）
   - 需要计算Gas成本是否值得
   - 建议只在预期收益大于成本时使用

7. **适用场景**：
   - ✅ 新币上线抢先交易
   - ✅ 高波动代币快速套利
   - ✅ 测试代币流动性
   - ❌ 不适合长期持有策略
   - ❌ 不适合低波动代币

---

## 🔥 功能4：买入量触发卖出（可自由调节）

### 功能说明

监控所有地址（除了您自己）的买入交易，当有人的买入金额超过您设定的阈值时，自动卖出您持有的该代币。**阈值可以自由设置。**

### 应用场景

1. **防止被砸盘**：当检测到大额买入（可能是巨鲸），立即卖出获利
2. **趋势判断**：大资金进场可能预示价格上涨，及时套现
3. **风险控制**：避免在大额交易后的价格波动中被套

### 配置参数

在 `.env` 文件中添加以下配置：

```env
# 是否启用买入量触发卖出 (true/false)
SELL_ON_BUY_VOLUME_ENABLED=true

# 买入量触发阈值 (BNB，可自由设置任意值)
# 当其他地址单笔买入超过此金额时，自动卖出您的持仓
SELL_ON_BUY_VOLUME_THRESHOLD=1.0
```

### 参数说明

| 参数 | 说明 | 默认值 | 示例 |
|------|------|--------|------|
| `SELL_ON_BUY_VOLUME_ENABLED` | 是否启用买入量触发卖出 | `false` | `true` |
| `SELL_ON_BUY_VOLUME_THRESHOLD` | 触发阈值（BNB）（**可自由设置**） | `1.0` | `0.5`, `2.0`, `5.0` 等任意值 |

### 使用示例

#### 示例1：低阈值（敏感）
```env
SELL_ON_BUY_VOLUME_ENABLED=true
SELL_ON_BUY_VOLUME_THRESHOLD=0.5    # 有人买入超过0.5 BNB就卖
```

**特点**：
- 对买入非常敏感
- 可能频繁触发卖出
- 适合快进快出策略

#### 示例2：中等阈值（平衡）
```env
SELL_ON_BUY_VOLUME_ENABLED=true
SELL_ON_BUY_VOLUME_THRESHOLD=1.0    # 有人买入超过1 BNB就卖
```

**特点**：
- 平衡敏感度和稳定性
- 只在有明显买入时触发
- 适合大多数场景

#### 示例3：高阈值（等待大户）
```env
SELL_ON_BUY_VOLUME_ENABLED=true
SELL_ON_BUY_VOLUME_THRESHOLD=5.0    # 有人买入超过5 BNB才卖
```

**特点**：
- 只在巨鲸买入时触发
- 触发频率低
- 适合等待大资金进场

#### 示例4：超高阈值（极端保守）
```env
SELL_ON_BUY_VOLUME_ENABLED=true
SELL_ON_BUY_VOLUME_THRESHOLD=10.0   # 有人买入超过10 BNB才卖
```

### 工作原理

1. **实时监控**：机器人每隔一段时间检查最新区块的所有交易
2. **过滤交易**：
   - 排除您自己的交易
   - 只关注PancakeSwap Router的交易
   - 识别买入交易（swapExactETHForTokens）
3. **计算金额**：提取交易的BNB金额
4. **判断触发**：如果金额 > 阈值，触发卖出
5. **解析代币**：从交易中解析出购买的代币地址
6. **检查持仓**：确认您是否持有该代币
7. **自动卖出**：如果持有，立即卖出全部持仓

### 日志示例

```
[info]: 启动买入量监控... { threshold: '1.0 BNB' }
[info]: 检测到大额买入，超过阈值！ { 
  from: '0x123...', 
  amount: '2.5 BNB', 
  threshold: '1.0 BNB',
  hash: '0xabc...' 
}
[info]: 触发自动卖出！ { 
  token: '0xdef...', 
  reason: '检测到2.5 BNB大额买入',
  holdingAmount: '1000000000000000000' 
}
[info]: 大额买入触发卖出成功 { 
  token: '0xdef...', 
  hash: '0x456...' 
}
```

### ⚠️ 重要提醒

1. **监控范围**：
   - 监控所有地址（除了您自己）
   - 不限于特定钱包
   - 任何符合条件的买入都会触发

2. **触发时机**：
   - 检测有延迟（取决于MONITOR_INTERVAL）
   - 可能错过最佳卖出价格
   - 建议设置合理的监控间隔

3. **代币解析**：
   - 使用交易日志解析代币地址
   - 大多数情况准确
   - 复杂交易可能无法识别

4. **持仓检查**：
   - 只卖出您持有的代币
   - 如果没有持仓，不会触发任何操作
   - 自动从持仓列表中移除已卖出的代币

5. **与其他功能的关系**：
   - 可以与止盈止损同时启用
   - 谁先触发就先执行谁
   - 卖出后持仓会被清除

---

## 🔄 四个功能结合使用

### 完整配置示例

```env
# ========== 基础配置 ==========
PRIVATE_KEY=your_private_key_here
AUTO_TRADE_ENABLED=true
MAX_BUY_AMOUNT=0.2
MIN_LIQUIDITY_BNB=5
GAS_PRICE_MULTIPLIER=2.0       # 快速交易建议提高

# ========== 止盈止损配置（可自由调节） ==========
TAKE_PROFIT_ENABLED=true
TAKE_PROFIT_PERCENT=100        # 盈利100%卖出（可改为50, 200等）
STOP_LOSS_PERCENT=30           # 亏损30%止损（可改为20, 50等）
CHECK_PROFIT_INTERVAL=60000    # 每分钟检查

# ========== 快速卖出配置（可自由调节） ==========
QUICK_SELL_ENABLED=false       # 与其他功能互斥，谨慎启用
QUICK_SELL_DELAY_MIN=1000      # 1秒
QUICK_SELL_DELAY_MAX=5000      # 5秒

# ========== 买入量触发卖出（可自由调节） ==========
SELL_ON_BUY_VOLUME_ENABLED=true
SELL_ON_BUY_VOLUME_THRESHOLD=2.0   # 有人买入超过2 BNB就卖（可改为0.5, 5.0等）

# ========== 钱包跟单配置 ==========
COPY_TRADE_ENABLED=true
MONITORED_WALLETS=0x1234567890123456789012345678901234567890
COPY_TRADE_AMOUNT=0.05
COPY_TRADE_MULTIPLIER=1.0
```

### 四重保护工作流程

1. **买入阶段**：
   - 新币上线自动购买
   - 或跟单目标钱包购买
   - 记录持仓信息

2. **持仓监控**（四个触发条件，任一满足即卖出）：
   - **快速卖出触发**：购买后1-5秒内自动卖出（优先级最高）
   - **止盈触发**：盈利达到设定百分比（如100%）
   - **止损触发**：亏损达到设定百分比（如30%）
   - **买入量触发**：检测到大额买入（如>2 BNB）

3. **卖出执行**：
   - 自动授权代币
   - 执行卖出交易
   - 清除持仓记录
   - 记录详细日志

### 策略组合示例

#### 组合1：极速套利（使用快速卖出）
```env
QUICK_SELL_ENABLED=true
QUICK_SELL_DELAY_MIN=1000      # 1秒
QUICK_SELL_DELAY_MAX=3000      # 3秒
TAKE_PROFIT_ENABLED=false      # 关闭止盈止损
SELL_ON_BUY_VOLUME_ENABLED=false   # 关闭买入量触发
GAS_PRICE_MULTIPLIER=3.0       # 高Gas确保快速成交
SLIPPAGE_TOLERANCE=20          # 高滑点容忍
```

#### 组合2：快进快出（不用快速卖出）
```env
QUICK_SELL_ENABLED=false       # 不用快速卖出
TAKE_PROFIT_PERCENT=30         # 低止盈
STOP_LOSS_PERCENT=20           # 低止损
SELL_ON_BUY_VOLUME_THRESHOLD=0.5   # 低阈值，敏感
```

#### 组合3：稳健持有
```env
QUICK_SELL_ENABLED=false       # 不用快速卖出
TAKE_PROFIT_PERCENT=100        # 高止盈
STOP_LOSS_PERCENT=40           # 中等止损
SELL_ON_BUY_VOLUME_THRESHOLD=3.0   # 高阈值
```

#### 组合4：追求暴利
```env
QUICK_SELL_ENABLED=false       # 不用快速卖出
TAKE_PROFIT_PERCENT=300        # 极高止盈（4倍）
STOP_LOSS_PERCENT=60           # 高止损
SELL_ON_BUY_VOLUME_THRESHOLD=10.0  # 极高阈值
```

---

## 📚 常见问题（更新）

### Q: 所有百分比和阈值都可以自由设置吗？
A: **是的！** 所有参数都可以自由调节：
- `TAKE_PROFIT_PERCENT`：可以设置任意值，如 30, 50, 100, 200, 500 等
- `STOP_LOSS_PERCENT`：可以设置任意值，如 10, 20, 30, 50 等
- `SELL_ON_BUY_VOLUME_THRESHOLD`：可以设置任意值，如 0.1, 0.5, 1.0, 5.0, 10.0 等
- `QUICK_SELL_DELAY_MIN/MAX`：可以设置任意值，如 500, 1000, 2000, 5000, 10000 毫秒等

### Q: 快速卖出和其他功能可以同时启用吗？
A: 可以，但**不建议**。快速卖出会在1-5秒内卖出，如果同时启用其他功能：
- 快速卖出优先级最高，会先执行
- 其他功能的定时器会被取消
- 建议只启用快速卖出，或只启用其他功能

### Q: 快速卖出适合什么场景？
A: 适合：
- ✅ 新币上线抢先交易
- ✅ 高波动代币快速套利
- ✅ 测试代币流动性
不适合：
- ❌ 长期持有策略
- ❌ 低波动代币

### Q: 买入量触发会监控所有地址吗？
A: 是的，除了您自己的地址外，所有其他地址的买入都会被监控。

### Q: 四个功能可以单独使用吗？
A: 可以！您可以：
- 只启用止盈止损
- 只启用快速卖出
- 只启用买入量触发
- 只启用跟单
- 或任意组合（但快速卖出建议单独用）

### Q: 如果多个条件同时满足会怎样？
A: 按检查顺序：
1. 快速卖出（定时触发，优先级最高）
2. 买入量触发（实时监控）
3. 止盈止损（定期检查）
第一个触发的会先执行卖出，卖出后持仓清除，其他条件不再触发。

### Q: 设置极端值会有问题吗？
A: 
- 极高止盈（如500%）：可能永远不会触发
- 极低止损（如5%）：可能频繁触发
- 极低买入阈值（如0.01 BNB）：可能频繁卖出
- 极短快速卖出（如500ms）：可能交易未确认就尝试卖出
建议根据实际情况设置合理值。

### Q: 快速卖出的延迟有多准确？
A: 延迟是在指定范围内随机的（如1000-5000ms），误差通常在几十毫秒内，但实际执行时间还受Gas价格和网络状况影响。

### Q: 买入量触发的延迟有多大？
A: 取决于 `MONITOR_INTERVAL`（默认1秒）。如果设置为500毫秒，延迟更小。

---

## ⚠️ 最后提醒

1. **测试先行**：
   - 先在测试网测试功能
   - 用小额资金在主网测试
   - 确认一切正常后再加大投入

2. **风险控制**：
   - 设置合理的止盈止损点位
   - 设置合理的买入量阈值
   - 不要过度依赖自动化
   - 保持对市场的独立判断

3. **持续监控**：
   - 定期查看日志
   - 检查持仓状态
   - 调整策略参数
   - 分析卖出效果

4. **技术限制**：
   - 所有功能都有延迟
   - 复杂交易可能识别不准确
   - 建议结合手动分析
   - 保持合理预期

5. **参数调优**：
   - 根据市场情况调整
   - 记录不同参数的效果
   - 找到最适合自己的策略
   - 定期回顾和优化

**祝您使用愉快！但请务必注意风险！** 🚀
