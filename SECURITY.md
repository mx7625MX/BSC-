# 安全指南

## 私钥安全

### ⚠️ 极其重要！

1. **永远不要分享您的私钥**
   - 不要通过邮件、聊天工具发送
   - 不要截图包含私钥的内容
   - 不要提交到Git仓库

2. **使用专用钱包**
   - 为机器人创建单独的钱包
   - 只存放必要的BNB数量
   - 定期转出利润到安全钱包

3. **环境变量保护**
   ```bash
   # .env 文件应该在 .gitignore 中
   # 永远不要提交 .env 到版本控制
   chmod 600 .env  # 限制文件权限（仅所有者可读写）
   ```

## 交易安全

### 防止蜜罐

1. **检查代币合约**
   - 使用 BSCCheck 或 TokenSniffer 验证
   - 查看合约源码是否开源
   - 检查是否有隐藏的买卖限制

2. **流动性锁定**
   - 检查LP代币是否锁定
   - 确认锁定时间
   - 警惕随时可能撤池的项目

3. **持币地址分析**
   - 检查前10大持币地址
   - 避免持币过于集中的项目
   - 警惕持币比例异常高的地址

### Gas费管理

1. **设置合理的Gas限制**
   ```env
   GAS_LIMIT=500000  # 避免过高或过低
   ```

2. **监控Gas价格**
   - 网络拥堵时调整策略
   - 使用Gas价格预言机
   - 设置最大可接受Gas价格

3. **失败交易处理**
   - 失败的交易仍会消耗Gas
   - 记录失败原因
   - 及时调整参数

## 服务器安全

### VPS/云服务器

1. **基础安全**
   ```bash
   # 更新系统
   sudo apt update && sudo apt upgrade
   
   # 配置防火墙
   sudo ufw allow 22/tcp
   sudo ufw enable
   
   # 禁用root登录
   sudo nano /etc/ssh/sshd_config
   # 设置 PermitRootLogin no
   ```

2. **SSH密钥认证**
   ```bash
   # 生成SSH密钥
   ssh-keygen -t rsa -b 4096
   
   # 禁用密码登录
   # 在 /etc/ssh/sshd_config 中设置
   # PasswordAuthentication no
   ```

3. **日志监控**
   ```bash
   # 定期检查系统日志
   sudo tail -f /var/log/syslog
   
   # 检查登录记录
   sudo last
   ```

## 代码安全

### 依赖管理

1. **审计依赖包**
   ```bash
   # 检查已知漏洞
   npm audit
   
   # 自动修复
   npm audit fix
   ```

2. **锁定版本**
   - 使用 package-lock.json
   - 定期更新依赖
   - 测试后再部署

### 访问控制

1. **环境隔离**
   - 测试网和主网分开
   - 使用不同的配置文件
   - 不同的钱包地址

2. **权限最小化**
   - 机器人只需要发送交易的权限
   - 不需要其他管理权限
   - 使用只读RPC节点（如果可能）

## 网络安全

### RPC节点

1. **选择可信节点**
   - 使用官方节点
   - 或自建节点
   - 避免不明来源的节点

2. **HTTPS连接**
   ```env
   # 始终使用HTTPS
   BSC_RPC_URL=https://bsc-dataseed1.binance.org/
   ```

3. **备用节点**
   ```typescript
   // 配置多个备用节点
   const RPC_URLS = [
     'https://bsc-dataseed1.binance.org/',
     'https://bsc-dataseed2.binance.org/',
     'https://bsc-dataseed3.binance.org/'
   ];
   ```

### DDoS防护

1. **速率限制**
   - 控制请求频率
   - 避免过于频繁的查询
   - 使用缓存减少请求

2. **错误处理**
   - 实现重试机制
   - 指数退避策略
   - 记录异常情况

## 资金安全

### 风险控制

1. **最大损失限制**
   ```env
   # 单次交易最大金额
   MAX_BUY_AMOUNT=0.1
   
   # 每日最大交易次数（自行实现）
   DAILY_TRADE_LIMIT=10
   ```

2. **止损策略**
   - 设置最大可接受损失
   - 定期评估收益
   - 及时调整策略

3. **分散投资**
   - 不要all in单个代币
   - 使用多个策略
   - 分散到不同项目

### 定期审计

1. **交易记录**
   - 保存所有交易hash
   - 定期对账
   - 分析成功率

2. **资金流向**
   - 记录入金/出金
   - 计算实际收益
   - 评估Gas成本

3. **性能评估**
   - 成功交易数
   - 失败原因分析
   - ROI计算

## 应急响应

### 发现异常

1. **立即停止机器人**
   ```bash
   # Ctrl+C 或
   kill -9 <pid>
   ```

2. **检查交易**
   - 访问 BscScan 查看交易
   - 确认资金状态
   - 识别异常交易

3. **更换钱包**
   - 如果怀疑私钥泄露
   - 立即转移资金
   - 生成新钱包

### 事件响应流程

1. **识别问题**
   - 查看日志
   - 分析错误信息
   - 确定影响范围

2. **隔离影响**
   - 停止自动交易
   - 保护现有资金
   - 防止进一步损失

3. **解决问题**
   - 修复代码/配置
   - 测试验证
   - 谨慎重启

4. **事后分析**
   - 记录事件详情
   - 更新安全措施
   - 预防类似问题

## 检查清单

### 部署前

- [ ] 已在测试网测试
- [ ] 配置文件已正确设置
- [ ] 私钥安全存储
- [ ] .env 文件权限正确
- [ ] 钱包有足够BNB
- [ ] 日志目录可写
- [ ] 了解所有风险

### 运行中

- [ ] 定期查看日志
- [ ] 监控资金变化
- [ ] 检查交易成功率
- [ ] 更新黑名单
- [ ] 评估策略效果

### 定期维护

- [ ] 更新依赖包
- [ ] 审计安全性
- [ ] 备份配置和日志
- [ ] 优化参数
- [ ] 转移利润

---

**安全永远是第一位的！**